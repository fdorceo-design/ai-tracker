# AI Tracker — notes for Claude

See **[AGENTS.md](AGENTS.md)** — that's where this project's actual
AI-operation conventions live now (composition/API rules, keyswitch timing,
launch order, reference docs). Kept as a separate file only because Claude
Code auto-loads a file with this exact name; the content isn't
Claude-specific, so it lives under the more universal name instead of being
duplicated here.
